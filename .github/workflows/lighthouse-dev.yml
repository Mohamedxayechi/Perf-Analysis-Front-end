name: Lighthouse Audit on PR (ng serve)

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Install dependencies
      run: npm install

    - name: Start Angular dev server
      run: |
        npm run start > server.log 2>&1 &
        SERVER_PID=$!
        echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
        echo "Angular server starting..."
        timeout 120 npx wait-on http://localhost:4200 || {
            echo "❌ Server failed to start within 2 minutes"
            cat server.log
            exit 1
        }
      env:
        HOST: 0.0.0.0
        PORT: 4200

    - name: Install Lighthouse
      run: npm install -g lighthouse

    - name: Run Lighthouse
      run: |
        lighthouse http://localhost:4200 \
          --output=json \
          --output-path=lighthouse-report.json \
          --chrome-flags="--headless --no-sandbox"

    - name: Extract Lighthouse Scores
      id: extract_scores
      run: |
        if [ -f lighthouse-report.json ]; then
          PERFORMANCE=$(jq '.categories.performance.score' lighthouse-report.json)
          ACCESSIBILITY=$(jq '.categories.accessibility.score' lighthouse-report.json)
          BEST_PRACTICES=$(jq '.categories["best-practices"].score' lighthouse-report.json)
          SEO=$(jq '.categories.seo.score' lighthouse-report.json)
          echo "PERFORMANCE=$PERFORMANCE" >> $GITHUB_OUTPUT
          echo "ACCESSIBILITY=$ACCESSIBILITY" >> $GITHUB_OUTPUT
          echo "BEST_PRACTICES=$BEST_PRACTICES" >> $GITHUB_OUTPUT
          echo "SEO=$SEO" >> $GITHUB_OUTPUT
        else
          echo "❌ Lighthouse JSON report not found"
          exit 1
        fi

    - name: Post Comment on PR
      uses: actions/github-script@v6
      with:
        script: |
          const { PERFORMANCE, ACCESSIBILITY, BEST_PRACTICES, SEO } = process.env;
          const body = `
          ### Lighthouse Audit Results

          - **Performance**: ${PERFORMANCE}
          - **Accessibility**: ${ACCESSIBILITY}
          - **Best Practices**: ${BEST_PRACTICES}
          - **SEO**: ${SEO}

          _This comment was generated by GitHub Actions._
          `;
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: body
          });
      env:
        PERFORMANCE: ${{ steps.extract_scores.outputs.PERFORMANCE }}
        ACCESSIBILITY: ${{ steps.extract_scores.outputs.ACCESSIBILITY }}
        BEST_PRACTICES: ${{ steps.extract_scores.outputs.BEST_PRACTICES }}
        SEO: ${{ steps.extract_scores.outputs.SEO }}

    - name: Enforce Performance Threshold
      run: |
        if [ -n "${{ steps.extract_scores.outputs.PERFORMANCE }}" ] && (( $(echo "${{ steps.extract_scores.outputs.PERFORMANCE }} < 0.9" | bc -l) )); then
          echo "❌ Performance score below 0.9 threshold"
          exit 1
        fi

    - name: Upload Report Artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: lighthouse-report
        path: lighthouse-report.json

    - name: Terminate Angular Server
      if: always()
      run: kill $SERVER_PID || echo "No process to kill"