export const environment = {
    httpProtocol: 'http://',      // Optional: Keep HTTP if you want to test it
    httpsProtocol: 'https://',    // Use HTTPS for secure communication
    baseUrl: 'localhost:5252',    
    

  };
